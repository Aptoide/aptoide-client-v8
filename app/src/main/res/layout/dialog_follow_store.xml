<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    >
  <View
      android:id="@+id/header_view"
      android:layout_width="0dp"
      android:layout_height="82.7dp"
      android:background="@color/red"
      app:layout_constraintLeft_toLeftOf="parent"
      app:layout_constraintRight_toRightOf="parent"
      app:layout_constraintTop_toTopOf="parent"
      />
  <ImageView
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:src="@drawable/follow_store_dialog_header_background"
      app:layout_constraintBottom_toBottomOf="@id/header_view"
      app:layout_constraintHorizontal_bias="1.0"
      app:layout_constraintLeft_toLeftOf="@id/header_view"
      app:layout_constraintRight_toRightOf="@id/header_view"
      app:layout_constraintTop_toTopOf="@id/header_view"
      app:layout_constraintVertical_bias="1.0"
      />

  <ImageView
      android:id="@+id/store_avatar"
      android:layout_width="104.1dp"
      android:layout_height="104.1dp"
      android:elevation="15dp"
      app:layout_constraintBottom_toBottomOf="@id/header_view"
      app:layout_constraintLeft_toLeftOf="parent"
      app:layout_constraintRight_toRightOf="parent"
      app:layout_constraintTop_toBottomOf="@id/header_view"
      app:srcCompat="@drawable/icon_user"
      />

  <TextView
      android:id="@+id/follow_store_text"
      android:layout_width="0dp"
      android:layout_height="wrap_content"
      android:layout_marginLeft="20dp"
      android:layout_marginRight="20dp"
      android:layout_marginTop="15dp"
      android:gravity="center"
      app:layout_constraintLeft_toLeftOf="parent"
      app:layout_constraintRight_toRightOf="parent"
      app:layout_constraintTop_toBottomOf="@+id/store_avatar"
      tools:text="Want to follow Chromalife store and get more updates?"
      style="@style/Aptoide.TextView.Regular.M"
      />

</android.support.constraint.ConstraintLayout>