val defaultCompileSdkVersion by extra { 34 }
val defaultMinSdkVersion by extra { 21 }

val defaultKotlinCompilerExtensionVersion by extra { "1.4.8" }

val defaultSupportedSdkVersion by extra { "3" }

val defaultCoreKtsVersion by extra { "1.12.0" }
val defaultKotlinxCoroutinesAndroidVersion by extra { "1.7.1" }

// Hilt
val defaultHiltAndroidVersion by extra { "2.46.1" }
val defaultDaggerHiltCompilerVersion by extra { "2.46.1" }

// Apache
val defaultApacheCommonsTextVersion by extra { "1.9" }

// Android Material
val defaultAndroidMaterialVersion by extra { "1.11.0" }

// Adyen
val defaultAdyenVersion by extra { "4.13.3" }

// Compose
val defaultNavigationComposeVersion by extra { "2.7.5" }
val defaultHiltNavigationComposeVersion by extra { "1.0.0" }
val defaultComposeMaterialVersion by extra { "1.5.4" }
val defaultLifecycleViewModelComposeVersion by extra { "2.6.1" }
