val compileSdkVersion by extra { 34 }
val minSdkVersion by extra { 21 }

// Billing SDK
val supportedSdkVersion by extra { "3" }

// Kotlin
val kotlinCompilerExtensionVersion by extra { "1.4.8" }

val coreKtsVersion by extra { "1.12.0" }
val kotlinxCoroutinesAndroidVersion by extra { "1.7.1" }

// Hilt
val hiltAndroidVersion by extra { "2.46.1" }
val daggerHiltCompilerVersion by extra { "2.46.1" }

// Apache
val apacheCommonsTextVersion by extra { "1.9" }

//Activity
val androidxActivityVersion by extra { "1.8.2" }

// Adyen
val adyenVersion by extra { "4.13.3" }

// Compose
val navigationComposeVersion by extra { "2.7.5" }
val hiltNavigationComposeVersion by extra { "1.0.0" }
val composeMaterialVersion by extra { "1.5.4" }
val lifecycleViewModelComposeVersion by extra { "2.6.1" }

//Annotations
val androidxAnnotationVersion by extra { "1.7.1" }
