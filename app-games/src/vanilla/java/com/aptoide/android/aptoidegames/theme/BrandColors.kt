package com.aptoide.android.aptoidegames.theme

import androidx.compose.ui.graphics.Color

/**
 * Brand primary color for non-composable code (e.g. Notification builders) that
 * cannot call the @Composable [Palette]. Same value as `Palette.Primary` for
 * this flavor (it does not invert with theme). Vanilla = orange.
 */
internal val BrandPrimary = Color(0xFFFE6446)
